# QuicklIME 開発ロードマップ

作成日: 2026-07-11

## プロジェクト概要

- **QuicklIME**: Windows 用の自作日本語IME
- **目的**: 常用に耐える実用IMEを作ること（学習目的ではない）
- **入力方式**: 通常のローマ字入力。変換を工夫して様々なシーンでの高速入力を目指す

## アーキテクチャ

Mozc / Google日本語入力と同じ「薄いクライアント + 別プロセスエンジン」構成を採用する。

| 層 | 言語 | 形態 | 役割 |
|---|---|---|---|
| TSF層 | C++ | in-proc COM DLL | Windows に IME として登録、キー捕捉、composition 管理、候補描画 |
| 変換エンジン | Rust | 常駐別プロセス | ローマ字かな変換、かな漢字変換、学習 |
| (将来) 設定UI等 | 未定 | 別実行ファイル | 設定画面、ユーザ辞書管理 |

### この構成を選んだ理由

- TSF のテキストサービスは in-proc COM DLL であり、全アプリのプロセスにロードされるため、軽量・堅牢な C++ 以外の選択肢が実質ない
- エンジンを別プロセスにすることで:
  - 学習データを全アプリで共有できる
  - エンジンがクラッシュしてもホストアプリを巻き込まない
  - 32bit/64bit 問題がエンジン側では消える（DLL は両アーキテクチャ分必要）
  - エンジンの言語を自由に選べる → 品質重視で Rust を選択
- 検討した代替案: 全部C++（メモリ安全性とイテレーション速度で見送り）、C#エンジン（品質重視のため見送り。ただし設定UIには将来採用の可能性あり）

## 開発フェーズ

### フェーズ0: 環境構築

- [x] Visual Studio 2022 Community (C++ デスクトップ開発ワークロード) + Windows SDK 10.0.26100
- [x] Rust ツールチェーン (rustup 1.29.0 / rustc 1.97.0 stable-msvc)
- [x] git リポジトリの初期化
- [x] 参考コードの取得 (references/ 以下、git管理外)
  - Microsoft Windows-classic-samples の SampleIME (TSF実装の出発点)
  - CorvusSKK (最も読みやすい実戦的 TSF 実装。事実上の最良資料)

### フェーズ1: 最小TSF IME (C++のみ)

「IMEとして登録され、キーを押すとひらがなが確定される」だけのものを作る。

- [x] COM DLL としての自己登録 (regsvr32)、言語バー/入力方式一覧への表示
- [x] キーイベントの捕捉 (ITfKeyEventSink)
- [x] アプリへの文字列挿入

**完了条件: メモ帳で「か」と打てる** → 2026-07-12 達成 (かな直接入力を確認)

注: 全工程で一番の難所。動くまで達成感のない期間が続くが、越えれば後は積み上げ。

### フェーズ2: ローマ字変換と未確定文字列 (C++)

- [x] ローマ字 → かな変換テーブル (拗音・促音・ん・外来音・小書き文字対応)
- [x] composition (下線付き未確定文字列) の管理
- [x] 変換 (スペース)・確定 (Enter)・取消 (Esc) の状態機械
- [x] 候補ウィンドウの表示 (単純なポップアップ。候補は暫定でカタカナ/ひらがなの2つ)

2026-07-13 完了。変換候補の中身はフェーズ4でエンジンに差し替える。

### フェーズ3: Rustエンジンの分離

- [x] Rust側: named pipe サーバとして常駐するエンジンプロセス (interprocess クレート)
- [x] プロトコル設計 (docs/protocol.md。行ベースTSV、エンジン未起動時はフォールバック)
- [x] C++側の変換候補生成をエンジン呼び出しへ移譲 (EngineClient)
  - ローマ字→かな変換は C++ 側に残置。エンジンへ移すかはフェーズ4で判断

2026-07-13 完了。エンジン停止/再起動時のフォールバック・再接続も動作確認済み。

### フェーズ4: かな漢字変換エンジン本体 (Rust)

- [x] 辞書: Mozc OSS 辞書 (約129万エントリ) を流用。2026-07-16 に HashMap から
      fst (有限状態トランスデューサ) へ移行 (フェーズ4-14)。完全一致・前方一致・
      共通接頭辞検索を1つの構造で捌き、メモリ約60MB削減 (283MB→221MB)。
      Viterbi のラティス構築も部分文字列の総当たり lookup から
      共通接頭辞検索の1回走査に置き換えた
- [x] Viterbi による最小コスト経路探索 (連接行列対応)
- [x] 文節区切りと部分変換 (品詞ベースの文節化、←→で文節移動、文節別候補選択)
- [x] 学習 (確定履歴による候補順の調整)。%APPDATA%\QuicklIME\learning.tsv へ永続化
      (現状は「読み→最後に確定した表記」の単純方式。頻度ベース等への改良余地あり)
- [x] 文節単体の候補の充実 (先頭の自立語を入れ替えた候補。「きょうは」に「京は」等が出る)
- [x] 文節区切りの手動調整 (Shift+←→ での伸縮)。縮小時は境界より後ろを固定せず
      自由に再変換し、伸縮の往復で元の区切りに戻るようにした
- [x] 記号辞書 (Mozc の symbol.tsv を読み込み、「やじるし」→「→」「ほし」→「★」等。
      通常語の後ろに追記する方式で、候補数上限には数えない)
- [x] 英単語入力 (生ローマ字候補)。打鍵列をそのまま半角候補にする (「apple」と打つと
      apple / Apple / APPLE)。かなに変換できない英字は composition に残し (MS-IME 同様)、
      英字を含む入力は全体を1文節に固定して変換する
- [x] ファンクションキー変換。F6-F10 は ひらがな/全角カタカナ/半角カタカナ/全角英数/半角英数
      への直接変換 (TSF層でローカルに文字種変換。変換前は全文、変換中は現在文節が対象)。
      F4 は ATOK 風の記号変換 (エンジンの CONVSYM で記号辞書の候補のみに絞る)
- [x] 記号キーと Shift+英字の composition 化。直接確定をやめ、未確定文字列として
      入力してから変換できるようにした (「（」を変換すると ［ 〔 等)。記号辞書の照合は
      読みを全角→半角に正規化して行う。JIS 配列の記号キーを一通りサポート。
      Shift+英字は英字モードに入り、以降の英字はローマ字変換せずアルファベットのまま続く
- [x] 対記号の両側同期変換。かっこ (（）｛｝「」) やクオート (”’) の対を含む入力で
      片側の候補を変えると、対になる側の文節も対応する記号に同期する
      (開き↔閉じは文節読みの深さ照合、左右同形のクオートは交互ペアで対応付け)
- [ ] 高速入力の工夫 (予測変換、省入力など)。2026-07-15 に既存IME・自作IMEを調査して
      優先度を整理した (詳細は下記「高速入力機能の検討結果」)

#### 高速入力機能の検討結果 (2026-07-15 調査)

Google日本語入力 / ATOK / macOS標準 / Mozc / azooKey / Akaza を調査し、費用対効果で優先度づけした。

**優先度A: 効果大・現構成に素直に乗る**

- [x] 履歴ベース予測変換 (2026-07-16 実装、フェーズ4-11)。learning.tsv の確定履歴を
      前方一致検索し (追記ログの行順から新しさを導出)、読み2文字以上で composition 下に
      サジェスト表示。Tab/↓で選択、↑で戻る (先頭でさらに↑は解除)、Enter で確定 (+LEARN)、
      Esc は選択解除のみの二段階。変換中の Tab は変換を取り消してサジェスト選択へ移行。
      英字モード・英字を含む読みでも履歴が前方一致すればサジェストする (英単語の省入力)。
      英字を含む入力は無変換の Enter 確定でも読み=表記で学習し、履歴に蓄積する
      (かなのみの無変換確定は、変換候補の並びを崩すため学習しない)。
      composition 中の Tab・矢印キー・PgUp/PgDn は用途が無くても常に食べ、
      アプリにキャレットやフォーカスを動かさせない。
      プロトコルに PREDICT コマンドを追加
- [x] 辞書ベース予測 (2026-07-16 実装、フェーズ4-11)。当初はソート済み読み配列 +
      二分探索の前方一致検索で先行実装し、同日の fst 移行 (フェーズ4-14) で
      読み配列を廃止してメモリ増を解消した。
      候補は履歴 (新しい順) → 辞書 (コスト順) の合成で最大8件。
      リアルタイム変換サジェストは未着手
      (Google日本語入力のサジェストは「入力履歴・システム辞書・リアルタイム変換」の3ソース合成。
      Mozc は src/prediction/ が独立サブシステムで、ダブル配列トライの前方一致検索が土台)
- [x] 短縮よみ (2026-07-16 実装、フェーズ4-16)。%APPDATA%\QuicklIME\userdict.tsv
      (QUICKLIME_USER_DICT_FILE で上書き可) から品詞「短縮よみ」の行を読み込む。
      形式は Mozc ユーザ辞書エクスポート互換の「読み\t表記\t品詞\t[コメント]」TSV
      (品詞列なしも短縮よみ扱い、# 行はコメント)。PREDICT では最優先
      (短縮よみ → 履歴 → 辞書)、変換では読みが完全一致した文節の経路表記直後に入る
      (学習表記の先頭移動が最優先)。反映はエンジン再起動。
      F5 で短縮よみの候補のみに絞った変換もできる (CONVUSER。F4 の記号変換と同じ操作感)。
      登録UI・他品詞対応はフェーズ5のユーザ辞書登録で拡張する
      (ATOK の省入力データ相当。メールアドレスや定型文を数打鍵で出す)

**優先度B: 小さく作れて日々効く**

- [x] 日付・時刻の動的候補 (2026-07-15 実装)。「きょう」「あした (あす)」「あさって」
      「きのう」「おととい」「ことし」「らいねん」「きょねん」「いま」に現在日時の候補
      (2026/07/15、7月15日(水)、令和8年7月15日、14:30 など) を出す。
      エンジン側 (datetime.rs、chrono 使用)。当初は通常変換の候補に追記していたが、
      同音異義語の多い読みで候補が深く埋もれるため、F4 の特殊変換 (CONVSYM) に統合した
      (F4 で記号 → 日付の順に出る)。日付候補は学習から除外
      (翌日以降に古い日付が先頭に来るのを防ぐ)。数字表記変換 (「1234」→千二百三十四) は未着手
- [x] 確定アンドゥ (Ctrl+Backspace) (2026-07-15 実装)。直前の確定文字列と確定前の読み
      (コンポーザ) を覚えておき、composition が無いときの Ctrl+Backspace で確定文字列を
      削除して読みの composition に復元する。キャレット直前のテキストが確定文字列と
      一致するときのみ働き、一致しなければ何もしない (一度きりで記憶は消える)
- [ ] 再変換。確定済みテキストの選択範囲を再変換する (TSF の ITfFnReconversion 対応が必要でやや重い)

**優先度C: 効果はあるが好みや実装難度の壁**

- [x] ローマ字テーブルの外部ファイル化 + AZIK 風拡張 (2026-07-16 実装、フェーズ4-13)。
      %APPDATA%\QuicklIME\romaji.tsv (UTF-8 TSV、QUICKLIME_ROMAJI_FILE で上書き可) を
      既定テーブルへ追加・上書きマージする (かな欄が空の行は既定エントリの削除、
      3列目付きの Google 日本語入力形式の行は非対応として無視)。テーブルはプロセスごとに
      初回参照時に一度だけ読むため、反映はアプリ再起動後。AZIK 風拡張 (撥音拡張・
      二重母音拡張、平均約12%の打鍵削減とされる) はプリセット data/romaji-azik.tsv を
      コピーして任意適用。本家 AZIK の x=しゃ行 / q=ん は既定の小書き・くぁ行と
      衝突するため除外した
- [x] タイプミス補正 (2026-07-16 実装、フェーズ4-15)。読みかなの曖昧前方一致
      (1かな置換・隣接かな入れ替え・1かな脱落/余分、先頭かなは固定) を fst の
      ノード自前走査で実装し、PREDICT の完全一致候補が8件に満たないときだけ
      コスト順で補充する (Google日本語入力 v1.2 / ATOK 訂正学習相当)。
      打鍵列 (ローマ字) は TSF 層で消費されエンジンに届かないため、
      隣接キーモデルではなくかなレベルの編集距離1で近似した
      (母音の隣接キーミスや「ん」抜けはかな1文字の誤りに落ちる)。
      変換 (CONVERT/CONVSEG) への補正候補の追加は未着手
- [ ] ライブ変換 (オプション)。毎打鍵で変換して表示し Enter のみで確定 (macOS標準・azooKey 方式)。
      Viterbi があるので技術的には可能だが UX の好みが激しく分かれるため必ずオプションにする

**優先度D: 長期の研究枠 (フェーズ6以降)**

- [ ] ニューラル補正 (azooKey の Zenzai 方式)。GPT-2 系の小型モデルで変換し、既存の統計変換を
      ドラフトにした投機的デコーディングでリアルタイム性を確保する。現エンジンを
      ドラフトに使える点でアーキテクチャの相性は良いが、規模が別次元のため調査課題に留める

参考になる Rust クレート: vibrato (形態素解析)、lindera。辞書検索は fst を採用済み
(yada / crawdad のダブル配列は前方一致列挙の API が無く予測入力に使えないため見送り)

### フェーズ5: 実用化・常用開始

- [x] エンジンの自動起動 (TSF側から起動、二重起動防止付き。フェーズ4中に前倒しで実装)
- [ ] 設定UI
- [ ] ユーザ辞書登録
- [ ] インストーラ
- [ ] 32bit版DLLの同梱
- [ ] 自分で常用しながらの改善サイクルへ

## 開発上の注意点

- **自分のマシンで直接デバッグしない**。IME の DLL は全アプリにロードされるため、バグるとテキスト入力全体が壊れたり、DLL がロードされたままで更新できなくなったりする
  - 開発初期はテスト専用の小さなホストアプリ (メモ帳で十分) で試す
  - Microsoft IME をいつでも切り替えられる状態を維持する
  - VM (Hyper-V) 上でテストできるとさらに安全
- TSF の公式ドキュメントは薄い。動いている実装 (CorvusSKK) を読むのが最良の資料
- 起動しっぱなしの古いエンジンプロセスが同名パイプへの接続を受けてしまい、新しいコードの
  テスト結果が古い挙動になることがある。エンジンのテスト前は `tasklist | grep quicklime` で
  旧プロセスの有無を確認して終了しておく
- Windows 11 のメモ帳はウィンドウを閉じてもプロセスが残り、新しいウィンドウも同じプロセス
  (= 古い DLL をロードしたまま) で開く。TSF 層のテスト前は `taskkill /IM Notepad.exe /F` で
  プロセスごと終了してから開き直す
- 再ビルド時に DLL がプロセスにロックされてリンクに失敗する場合は、ロード中でもリネームは可能なので
  `mv QuicklIME.dll QuicklIME.dll.old` と退避してからリンクし直す。古い DLL を掴んだプロセスは
  再起動するまで旧版のまま動く点に注意 (テストは新しく起動したアプリで行う)

## 参考リンク

- Mozc: https://github.com/google/mozc
- CorvusSKK: https://github.com/nathancorvussolis/corvusskk
- Windows-classic-samples (SampleIME): https://github.com/microsoft/Windows-classic-samples
- ime-rs (SampleIME の Rust 移植例): https://github.com/microsoft/ime-rs
- Mozc の履歴予測実装: https://github.com/google/mozc/blob/master/src/prediction/user_history_predictor.cc
- Zenzai (ニューラルかな漢字変換) 解説: https://knowledge.sakura.ad.jp/42901/
- AZIK 総合解説書 (拡張ローマ字): http://www1.vecceed.ne.jp/~bemu/azik/azikinfo.htm
