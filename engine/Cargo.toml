[package]
name = "quicklime-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
# named pipe サーバ用 (Windows では \\.\pipe\ にマップされる)
interprocess = "2"
# 日付・時刻の動的候補用 (ローカル時刻の取得と日付計算)
chrono = "0.4"
